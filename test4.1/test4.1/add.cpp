#include <bits/stdc++.h>
using namespace std;
#define lowbit(x) (x&-x)
typedef long long ll;
const int N = 20;
int arr[N];

int main()
{
    ios::sync_with_stdio(0); cin.tie(0);
    vector<vector<int>>arr(10,vector<int>(5,10));

    return 0;
}